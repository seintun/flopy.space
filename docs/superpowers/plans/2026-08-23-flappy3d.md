# Flappy3D Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mobile-first portrait 3D flappy bird with time-manipulation mechanics (slow-mo orbs, death rewind, day/night) and a juicy addiction loop (combo, near-miss, feathers, skins).

**Architecture:** Hand-rolled fixed-timestep engine (120Hz physics, interpolated render) over Three.js views. Pure logic modules in `src/core/**` are headless (never import `three`) and unit-tested; mesh/view modules are thin state-sync layers. One `TimeSystem` owns `timeScale`; every mechanic scales through it.

**Tech Stack:** Vite, TypeScript (strict), Three.js, Vitest. No other runtime deps. No backend.

**Spec:** `docs/superpowers/specs/2026-08-23-flappy3d-design.md` — read it first; this plan argues from it.

## Global Constraints

- Mobile-first: portrait layout; whole-screen tap input via `pointerdown` (no click delay); `touch-action:none`; double-tap zoom disabled; safe-area insets respected.
- Perf budget: pixelRatio ≤ 2, ≤ 60k triangles, particle pool ≤ 200, no post-processing pipeline. Target 60fps mid-range phone.
- All SFX procedural WebAudio — zero audio asset downloads.
- Logic modules under `src/core/**` must NOT import `three`. Views read World state; they never own gameplay truth.
- Fixed timestep `DT = 1/120`; renderer receives interpolation alpha `acc / DT`.
- Forgiving hitbox: `HITBOX_RADIUS = 0.45 * 0.85 = 0.3825`.
- Persistence is localStorage only; every key prefixed `f3d.`.
- Every task ends with a commit. Run `npx vitest run` + `npm run build` before each commit.

---

## Phase 1 — Scaffold + Headless Core

### Task 0: Project scaffold

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html`, `src/main.ts`, `.gitignore`

**Interfaces:**
- Produces: npm scripts used by every later task: `npm run dev`, `npm run build`, `npx vitest run`.

- [ ] **Step 1: Scaffold Vite vanilla-ts project**

```bash
cd /Users/seintun/code/sandbox/ox-alpha
npm create vite@latest . -- --template vanilla-ts
npm install
npm install three
npm install -D vitest @types/three
```

If the directory is non-empty (docs/ exists), Vite may prompt — answer "Ignore files and continue". Ensure `package.json` scripts include `"test": "vitest run"`.

- [ ] **Step 2: Enforce strict tsconfig**

In `tsconfig.json` compilerOptions ensure: `"strict": true`, `"noUncheckedIndexedAccess": true`, `"moduleResolution": "bundler"`, `"target": "ES2022"`.

- [ ] **Step 3: Replace index.html shell**

```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <title>Flappy3D</title>
</head>
<body>
  <div id="app"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
</html>
```

- [ ] **Step 4: Minimal main.ts placeholder**

```ts
console.log("flappy3d boot");
```

- [ ] **Step 5: Verify + commit**

Run: `npx vitest run` → "No test files found" exit 0 (add `"test": "vitest run", "passWithNoTests": true` handling or create empty `src/core/__tests__/.gitkeep`). Then `npm run build` must succeed.

```bash
git add -A && git commit -m "chore: vite+ts+three scaffold"
```

### Task 1: Constants, types, seeded RNG

**Files:**
- Create: `src/core/constants.ts`, `src/core/types.ts`, `src/core/rand.ts`
- Test: `src/core/rand.test.ts`

**Interfaces:**
- Produces: all constants below verbatim; types `World`, `BirdState`, `Pipe`, `Orb`, `HitType`; `worldRand(w): number` (mutates `w.rngState`); `mulberry32(seed)` for tests.

- [ ] **Step 1: Write failing RNG test**

```ts
// src/core/rand.test.ts
import { describe, it, expect } from "vitest";
import { mulberry32 } from "./rand";
import type { World } from "./types";

describe("mulberry32", () => {
  it("is deterministic per seed", () => {
    const a = mulberry32(42), b = mulberry32(42);
    expect([a(), a(), a()]).toEqual([b(), b(), b()]);
  });
});

describe("worldRand", () => {
  it("advances world rngState deterministically", () => {
    const w = { rngState: 7 } as World;
    const v1 = worldRand(w);
    expect(w.rngState).not.toBe(7);
    const w2 = { rngState: 7 } as World;
    expect(worldRand(w2)).toBe(v1);
  });
});
```

- [ ] **Step 2: Run to verify fail** — `npx vitest run src/core/rand.test.ts` → FAIL (modules missing)

- [ ] **Step 3: Implement**

```ts
// src/core/constants.ts
export const GRAVITY = -22;
export const FLAP_VELOCITY = 7.5;
export const TERMINAL_VY = -14;
export const GROUND_Y = -6;
export const CEILING_Y = 8;
export const BIRD_X = 0;
export const BIRD_VISUAL_RADIUS = 0.45;
export const HITBOX_RADIUS = 0.3825; // 85% forgiving hitbox
export const PIPE_RADIUS = 0.9;
export const PIPE_SPACING_DIST = 11;
export const GAP_START = 4.5;
export const GAP_MIN = 2.8;
export const GAP_SHRINK_END_SCORE = 40;
export const GAP_WANDER_MAX = 1.5;
export const BASE_SCROLL = 6;
export const SCROLL_RAMP = 0.15;
export const MAX_SCROLL = 12;
export const NEAR_MISS_MARGIN = 0.3;
export const COMBO_CAP = 5;
export const COMBO_PASSES_PER_STEP = 5;
export const FEATHER_EVERY_POINTS = 10;
export const REWINDS_MAX_PER_RUN = 3;
export const REWIND_SECONDS = 1.5;
export const DT = 1 / 120;
export const BUFFER_LEN = 180; // 1.5s at 120Hz
export const SLOWMO_SCALE = 0.35;
export const SLOWMO_HOLD_S = 3;
export const SLOWMO_EASE_S = 0.15;
export const ORB_EVERY_PIPES_MIN = 8;
export const ORB_EVERY_PIPES_MAX = 12;
export const MILESTONE_EVERY = 10;
export const SPAWN_X = BIRD_X + 16;
export const PITCH_SMOOTHING = 8;
export const INVULN_TICKS = 120; // 1s post-rewind shimmer
```

```ts
// src/core/types.ts
import { BASE_SCROLL, CEILING_Y, GROUND_Y, ORB_EVERY_PIPES_MIN } from "./constants";

export type HitType = "pipe" | "ground";

export interface BirdState {
  y: number; vy: number; pitch: number; alive: boolean; invulnUntilTick: number;
}

export interface Pipe {
  id: number; x: number; gapCenter: number; gapHeight: number; scored: boolean;
}

export interface Orb {
  id: number; x: number; y: number; taken: boolean;
}

export interface World {
  tick: number;
  dist: number;
  scrollSpeed: number;
  bird: BirdState;
  pipes: Pipe[];
  orbs: Orb[];
  nextPipeId: number;
  nextOrbId: number;
  lastGapCenter: number;
  nextPipeAtDist: number;
  nextOrbPipesIn: number; // pipes remaining until next orb
  score: number;
  combo: number;
  feathersRun: number;
  rewindsUsedRun: number;
  rngState: number;
}

export function createWorld(seed: number): World {
  return {
    tick: 0, dist: 0, scrollSpeed: BASE_SCROLL,
    bird: { y: 1.5, vy: 0, pitch: 0, alive: true, invulnUntilTick: 0 },
    pipes: [], orbs: [],
    nextPipeId: 1, nextOrbId: 1,
    lastGapCenter: 1,
    nextPipeAtDist: 18, // ~3s grace at speed 6
    nextOrbPipesIn: ORB_EVERY_PIPES_MIN,
    score: 0, combo: 0, feathersRun: 0, rewindsUsedRun: 0,
    rngState: seed >>> 0,
  };
}
```

```ts
// src/core/rand.ts
import type { World } from "./types";

/** Serializable LCG — advances state inside the World so snapshots capture it. */
export function worldRand(w: World): number {
  w.rngState = (Math.imul(w.rngState, 1664525) + 1013904223) >>> 0;
  return w.rngState / 4294967296;
}

export function mulberry32(seed: number): () => number {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
```

Note: `createWorld` imports constants only — remove the unused import lint if flagged.

- [ ] **Step 4: Run tests** — `npx vitest run src/core/rand.test.ts` → PASS

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(core): constants, world types, seeded RNG"
```

### Task 2: Physics (stepBird, flap)

**Files:**
- Create: `src/core/physics.ts`
- Test: `src/core/physics.test.ts`

**Interfaces:**
- Consumes: constants, `World` from Task 1.
- Produces: `flap(w: World): void` (sets `vy = FLAP_VELOCITY` when alive); `stepBird(w: World, dt: number): void` (semi-implicit Euler gravity, terminal clamp at TERMINAL_VY, ceiling clamp without death, pitch lerp).

- [ ] **Step 1: Write failing tests**

```ts
// src/core/physics.test.ts
import { describe, it, expect } from "vitest";
import { flap, stepBird } from "./physics";
import { createWorld } from "./types";
import {
  FLAP_VELOCITY, GRAVITY, TERMINAL_VY, DT, CEILING_Y, HITBOX_RADIUS,
} from "./constants";

describe("stepBird", () => {
  it("matches analytic arc after a flap within tolerance", () => {
    const w = createWorld(1);
    flap(w);
    const t = 0.5;
    const steps = Math.round(t / DT);
    for (let i = 0; i < steps; i++) stepBird(w, DT);
    const expected = 1.5 + FLAP_VELOCITY * t + 0.5 * GRAVITY * t * t;
    expect(Math.abs(w.bird.y - expected)).toBeLessThan(0.05);
  });

  it("clamps fall speed at terminal velocity", () => {
    const w = createWorld(1);
    w.bird.vy = -30;
    stepBird(w, DT);
    expect(w.bird.vy).toBeGreaterThanOrEqual(TERMINAL_VY);
  });

  it("clamps position at ceiling without dying", () => {
    const w = createWorld(1);
    w.bird.y = CEILING_Y + 10;
    stepBird(w, DT);
    expect(w.bird.alive).toBe(true);
    expect(w.bird.y).toBeLessThanOrEqual(CEILING_Y - HITBOX_RADIUS);
  });

  it("eases pitch toward dive angle while falling", () => {
    const w = createWorld(1);
    w.bird.vy = TERMINAL_VY;
    for (let i = 0; i < 240; i++) stepBird(w, DT);
    expect(w.bird.pitch).toBeLessThan(-80);
  });
});
```

- [ ] **Step 2: Run to verify fail** — `npx vitest run src/core/physics.test.ts` → FAIL

- [ ] **Step 3: Implement**

```ts
// src/core/physics.ts
import {
  GRAVITY, FLAP_VELOCITY, TERMINAL_VY, CEILING_Y, HITBOX_RADIUS, PITCH_SMOOTHING,
} from "./constants";
import type { World } from "./types";

export function flap(w: World): void {
  if (!w.bird.alive) return;
  w.bird.vy = FLAP_VELOCITY;
}

export function stepBird(w: World, dt: number): void {
  const b = w.bird;
  b.vy = Math.max(TERMINAL_VY, b.vy + GRAVITY * dt);
  b.y += b.vy * dt;
  if (b.y > CEILING_Y - HITBOX_RADIUS) b.y = CEILING_Y - HITBOX_RADIUS;
  // target pitch: -25deg rising, easing to -90deg at terminal dive
  const frac = Math.min(1, Math.max(0, -b.vy / -TERMINAL_VY));
  const targetDeg = b.vy >= 0 ? -25 : -(25 + 65 * frac);
  const k = Math.min(1, PITCH_SMOOTHING * dt);
  b.pitch += (targetDeg - b.pitch) * k;
}
```

- [ ] **Step 4: Run tests** — PASS

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(core): bird physics — gravity, flap impulse, terminal clamp, pitch lerp"
```

### Task 3: Difficulty curves

**Files:**
- Create: `src/core/difficulty.ts`
- Test: `src/core/difficulty.test.ts`

**Interfaces:**
- Produces: `scrollForScore(score: number): number`; `gapForScore(score: number): number`

- [ ] **Step 1: Failing tests**

```ts
// src/core/difficulty.test.ts
import { describe, it, expect } from "vitest";
import { scrollForScore, gapForScore } from "./difficulty";
import { BASE_SCROLL, MAX_SCROLL, GAP_START, GAP_MIN, GAP_SHRINK_END_SCORE } from "./constants";

describe("scrollForScore", () => {
  it("starts at base and caps at max", () => {
    expect(scrollForScore(0)).toBe(BASE_SCROLL);
    expect(scrollForScore(1000)).toBe(MAX_SCROLL);
  });
  it("ramps monotonically", () => {
    for (let s = 1; s <= 60; s++) expect(scrollForScore(s)).toBeGreaterThanOrEqual(scrollForScore(s - 1));
  });
});

describe("gapForScore", () => {
  it("shrinks linearly from start to min by score 40 then holds", () => {
    expect(gapForScore(0)).toBe(GAP_START);
    expect(gapForScore(GAP_SHRINK_END_SCORE)).toBeCloseTo(GAP_MIN);
    expect(gapForScore(100)).toBe(GAP_MIN);
    expect(gapForScore(20)).toBeCloseTo((GAP_START + GAP_MIN) / 2);
  });
});
```

- [ ] **Step 2: Verify fail**, then implement:

```ts
// src/core/difficulty.ts
import { BASE_SCROLL, MAX_SCROLL, SCROLL_RAMP, GAP_START, GAP_MIN, GAP_SHRINK_END_SCORE } from "./constants";

export function scrollForScore(score: number): number {
  return Math.min(MAX_SCROLL, BASE_SCROLL + SCROLL_RAMP * Math.max(0, score));
}

export function gapForScore(score: number): number {
  const t = Math.min(1, Math.max(0, score) / GAP_SHRINK_END_SCORE);
  return GAP_START - (GAP_START - GAP_MIN) * t;
}
```

- [ ] **Step 3: Tests pass + commit**

```bash
git add -A && git commit -m "feat(core): difficulty curves — scroll ramp, gap shrink"
```

### Task 4: Spawner (pooled pipes/orbs, random-walk gaps)

**Files:**
- Create: `src/core/spawner.ts`
- Test: `src/core/spawner.test.ts`

**Interfaces:**
- Consumes: `worldRand`, difficulty fns, constants.
- Produces: `advance(w: World, dt: number): void` — scrolls dist/pipes/orbs leftward, recycles offscreen (keeps ≤ ~6 live pipes), spawns pipe when `dist >= nextPipeAtDist` (then `nextPipeAtDist += PIPE_SPACING_DIST`), spawns orb between new and previous pipe every `nextOrbPipesIn` pipes (recount 8–12 via rng).

- [ ] **Step 1: Failing property test**

```ts
// src/core/spawner.test.ts
import { describe, it, expect } from "vitest";
import { advance } from "./spawner";
import { createWorld } from "./types";
import { DT, GAP_WANDER_MAX, PIPE_SPACING_DIST, GROUND_Y, CEILING_Y } from "./constants";

function runSeconds(w: ReturnType<typeof createWorld>, secs: number) {
  for (let i = 0; i < secs * 120; i++) advance(w, DT);
}

describe("spawner", () => {
  it("spawns pipes at fixed distance intervals", () => {
    const w = createWorld(1);
    runSeconds(w, 40);
    expect(w.pipes.length).toBeGreaterThan(2);
    const xs = [...w.pipes].sort((a, b) => a.x - b.x);
    for (let i = 1; i < xs.length; i++) {
      expect(xs[i]!.x - xs[i - 1]!.x).toBeCloseTo(PIPE_SPACING_DIST, 5);
    }
  });

  it("property: consecutive gap centers wander <= GAP_WANDER_MAX and always fit play band (10k seeds)", () => {
    for (let seed = 0; seed < 10000; seed++) {
      const w = createWorld(seed);
      runSeconds(w, 25);
      let prev = w.lastGapCenter;
      const sorted = [...w.spawnHistory]; // spawnHistory recorded by spawner for testability
      for (const gc of sorted) {
        expect(Math.abs(gc - prev)).toBeLessThanOrEqual(GAP_WANDER_MAX + 1e-9);
        prev = gc;
      }
      for (const p of w.pipes) {
        expect(p.gapCenter - p.gapHeight / 2).toBeGreaterThan(GROUND_Y);
        expect(p.gapCenter + p.gapHeight / 2).toBeLessThan(CEILING_Y);
      }
    }
  });

  it("recycles offscreen pipes (pool stays small)", () => {
    const w = createWorld(1);
    runSeconds(w, 120);
    expect(w.pipes.length).toBeLessThan(8);
  });
});
```

- [ ] **Step 2: Verify fail. Step 3: Implement**

```ts
// src/core/spawner.ts
import {
  SPAWN_X, PIPE_SPACING_DIST, PIPE_RADIUS, GAP_WANDER_MAX, GROUND_Y, CEILING_Y,
  ORB_EVERY_PIPES_MIN, ORB_EVERY_PIPES_MAX, DESPAWN_MARGIN,
} from "./constants";
import { worldRand } from "./rand";
import { gapForScore } from "./difficulty";
import type { World } from "./types";

export function advance(w: World, dt: number): void {
  const dx = w.scrollSpeed * dt;
  w.dist += dx;
  for (const p of w.pipes) p.x -= dx;
  for (const o of w.orbs) o.x -= dx;

  const despawnX = SPAWN_X - 40; // far left of view
  void despawnX;
  while (w.pipes.length && w.pipes[0]!.x < BIRD_DESPAWN_X) w.pipes.shift();
  while (w.orbs.length && w.orbs[0]!.x < BIRD_DESPAWN_X) w.orbs.shift();

  while (w.dist >= w.nextPipeAtDist) {
    spawnPipe(w);
    w.nextPipeAtDist += PIPE_SPACING_DIST;
  }
}

const BIRD_DESPAWN_X = -SPAWN_X; // symmetric offscreen bound

function gapBounds(gapHeight: number): { lo: number; hi: number } {
  const half = gapHeight / 2;
  return { lo: GROUND_Y + 1.2 + half, hi: CEILING_Y - 1.2 - half };
}

function spawnPipe(w: World): void {
  const gh = gapForScore(w.score);
  const { lo, hi } = gapBounds(gh);
  const delta = (worldRand(w) * 2 - 1) * GAP_WANDER_MAX;
  const gc = Math.min(hi, Math.max(lo, w.lastGapCenter + delta));
  w.lastGapCenter = gc;
  w.pipes.push({ id: w.nextPipeId++, x: SPAWN_X, gapCenter: gc, gapHeight: gh, scored: false });

  if (--w.nextOrbPipesIn <= 0) {
    spawnOrb(w, gc);
    const range = ORB_EVERY_PIPES_MAX - ORB_EVERY_PIPES_MIN;
    w.nextOrbPipesIn = ORB_EVERY_PIPES_MIN + Math.floor(worldRand(w) * (range + 1));
  }
}

function spawnOrb(w: World, recentGapCenter: number): void {
  const y = Math.min(
    CEILING_Y - 1.5,
    Math.max(GROUND_Y + 1.5, recentGapCenter + (worldRand(w) * 2 - 1)),
  );
  w.orbs.push({ id: w.nextOrbId++, x: SPAWN_X - PIPE_SPACING_DIST / 2, y, taken: false });
}
```

Add to `constants.ts`: `export const DESPAWN_MARGIN = 2;` (or remove unused import). Also add `spawnHistory: number[]` to `World` (init `[]` in `createWorld`) and push each `gc` in `spawnPipe` — test hook, harmless in prod. Remove the stray `despawnX` lines above; use `BIRD_DESPAWN_X` constant directly.

- [ ] **Step 4: Tests pass** (property test may take seconds — fine). **Commit:**

```bash
git add -A && git commit -m "feat(core): pooled spawner with bounded random-walk gaps + clock orbs"
```

### Task 5: Collision

**Files:**
- Create: `src/core/collision.ts`
- Test: `src/core/collision.test.ts`

**Interfaces:**
- Consumes: constants, World.
- Produces: `sphereHitsAabb(cx,cy,cz,r, minx,miny,minz,maxx,maxy,maxz): boolean`; `checkCollisions(w: World): HitType | null` — returns `'ground'` if `bird.y - GROUND_Y <= HITBOX_RADIUS`, `'pipe'` on overlap with upper/lower pipe AABBs (x/z extent ±PIPE_RADIUS around pipe.x, lower box y∈[GROUND_Y−1, gapBottom], upper y∈[gapTop, CEILING_Y+1]); respects `invulnUntilTick` and `alive`.

- [ ] **Step 1: Failing tests**

```ts
// src/core/collision.test.ts
import { describe, it, expect } from "vitest";
import { sphereHitsAabb, checkCollisions } from "./collision";
import { createWorld } from "./types";
import { HITBOX_RADIUS, PIPE_RADIUS, GROUND_Y } from "./constants";

describe("sphereHitsAabb", () => {
  it("detects overlap and rejects edge-touch", () => {
    expect(sphereHitsAabb(0, 0, 0, 1, 2, -5, 5, 5, 5, 5)).toBe(false); // touching at x=1? no: closest x=2 dist>1... adjust values below
    expect(sphereHitsAabb(0, 0, 0, 1.5, 1, -1, -1, 2, 1, 1)).toBe(true);
  });
});

describe("checkCollisions", () => {
  it("ground kill", () => {
    const w = createWorld(1);
    w.bird.y = GROUND_Y + 0.1;
    expect(checkCollisions(w)).toBe("ground");
  });
  it("pipe body kill but gap center clear", () => {
    const w = createWorld(1);
    w.pipes.push({ id: 1, x: 0, gapCenter: 1, gapHeight: 4.5, scored: false });
    w.bird.y = 1; // center of gap
    expect(checkCollisions(w)).toBeNull();
    w.bird.y = 1 + 4.5 / 2 + HITBOX_RADIUS * 0.9; // just inside top pipe zone
    expect(checkCollisions(w)).toBe("pipe");
  });
  it("invulnerable ticks skip collision", () => {
    const w = createWorld(1);
    w.tick = 10;
    w.bird.invulnUntilTick = 50;
    w.bird.y = GROUND_Y;
    expect(checkCollisions(w)).toBeNull();
  });
});
```

Fix the first sphere/AABB case numbers so intent is unambiguous: touching case `sphereHitsAabb(0,0,0, 1, 1,-1,-1, 3,1,1)` → closest point (1,0,0), dist=1, r=1 → strict `<` means false (edge touch rejected). Overlap case shift box to x∈[0.5,…].

- [ ] **Step 2: Verify fail. Step 3: Implement**

```ts
// src/core/collision.ts
import { BIRD_X, GROUND_Y, HITBOX_RADIUS, PIPE_RADIUS } from "./constants";
import type { HitType, World } from "./types";

export function sphereHitsAabb(
  cx: number, cy: number, cz: number, r: number,
  minx: number, miny: number, minz: number, maxx: number, maxy: number, maxz: number,
): boolean {
  const nx = Math.max(minx, Math.min(cx, maxx));
  const ny = Math.max(miny, Math.min(cy, maxy));
  const nz = Math.max(minz, Math.min(cz, maxz));
  const dx = cx - nx, dy = cy - ny, dz = cz - nz;
  return dx * dx + dy * dy + dz * dz < r * r;
}

export function checkCollisions(w: World): HitType | null {
  const b = w.bird;
  if (!b.alive || w.tick < b.invulnUntilTick) return null;
  if (b.y - GROUND_Y <= HITBOX_RADIUS) return "ground";
  for (const p of w.pipes) {
    if (Math.abs(p.x - BIRD_X) > PIPE_RADIUS + HITBOX_RADIUS) continue;
    const gapTop = p.gapCenter + p.gapHeight / 2;
    const gapBot = p.gapCenter - p.gapHeight / 2;
    const minx = p.x - PIPE_RADIUS, maxx = p.x + PIPE_RADIUS;
    const lowerHit = sphereHitsAabb(BIRD_X, b.y, 0, HITBOX_RADIUS, minx, GROUND_Y - 1, -PIPE_RADIUS, maxx, gapBot, PIPE_RADIUS);
    const upperHit = sphereHitsAabb(BIRD_X, b.y, 0, HITBOX_RADIUS, minx, gapTop, -PIPE_RADIUS, maxx, CEILING_TOP, PIPE_RADIUS);
    if (lowerHit || upperHit) return "pipe";
  }
  return null;
}

const CEILING_TOP = 99; // effectively open-top upper pipe box
```

- [ ] **Step 4: Pass + commit** `feat(core): sphere-vs-AABB collisions with invulnerability`

### Task 6: Scoring, combo, feathers

**Files:**
- Create: `src/core/scoring.ts`
- Test: `src/core/scoring.test.ts`

**Interfaces:**
- Produces: `multiplier(combo: number): number` = `min(COMBO_CAP, 1 + floor(combo / COMBO_PASSES_PER_STEP))`; `processPasses(w: World): PassEvent[]` where `PassEvent = { pipeId: number; nearMiss: boolean; points: number }` — marks pipe scored when `x < BIRD_X − PIPE_RADIUS`; combo +1 (+2 near miss); points = multiplier(combo) applied AFTER combo bump; feather earned each crossing of a multiple of 10 (`feathersRun++`, uncapped here — spend cap enforced elsewhere); also updates `w.score` and returns events.

- [ ] **Step 1: Failing tests** — cover: first pass = 1pt; 5th consecutive clean pass → ×2; near-miss accelerates combo; feather cadence at score 10/20 crossing; already-scored pipes ignored.

```ts
// src/core/scoring.test.ts
import { describe, it, expect } from "vitest";
import { multiplier, processPasses } from "./scoring";
import { createWorld } from "./types";

describe("multiplier", () => {
  it("steps 1..5 and caps", () => {
    expect(multiplier(0)).toBe(1);
    expect(multiplier(4)).toBe(1);
    expect(multiplier(5)).toBe(2);
    expect(multiplier(20)).toBe(5);
    expect(multiplier(100)).toBe(5);
  });
});

describe("processPasses", () => {
  it("scores unscored pipe once, awards combo+points", () => {
    const w = createWorld(1);
    w.pipes.push({ id: 1, x: -1, gapCenter: 1, gapHeight: 4.5, scored: false });
    w.bird.y = 1;
    const ev = processPasses(w);
    expect(ev).toHaveLength(1);
    expect(ev[0]).toMatchObject({ pipeId: 1, points: 1, nearMiss: false });
    expect(w.score).toBe(1);
    expect(processPasses(w)).toHaveLength(0); // not twice
  });
  it("near-miss bumps combo faster and grants bonus points", () => {
    const w = createWorld(1);
    w.pipes.push({ id: 1, x: -1, gapCenter: 1, gapHeight: 4.5, scored: false });
    w.bird.y = 1 + 4.5 / 2 + 0.15; // within NEAR_MISS_MARGIN of edge
    const ev = processPasses(w)[0]!;
    expect(ev.nearMiss).toBe(true);
    expect(w.combo).toBe(2);
  });
  it("feather every 10 points", () => {
    const w = createWorld(1);
    w.score = 9;
    w.feathersRun = 0;
    w.pipes.push({ id: 1, x: -1, gapCenter: 1, gapHeight: 9, scored: false });
    processPasses(w);
    expect(w.score).toBe(10);
    expect(w.feathersRun).toBe(1);
  });
});
```

- [ ] **Step 2–4:** verify fail → implement:

```ts
// src/core/scoring.ts
import { BIRD_X, PIPE_RADIUS, NEAR_MISS_MARGIN, COMBO_CAP, COMBO_PASSES_PER_STEP, FEATHER_EVERY_POINTS } from "./constants";
import type { World } from "./types";

export interface PassEvent { pipeId: number; nearMiss: boolean; points: number }

export function multiplier(combo: number): number {
  return Math.min(COMBO_CAP, 1 + Math.floor(Math.max(0, combo) / COMBO_PASSES_PER_STEP));
}

export function processPasses(w: World): PassEvent[] {
  const events: PassEvent[] = [];
  for (const p of w.pipes) {
    if (p.scored || p.x >= BIRD_X - PIPE_RADIUS) continue;
    p.scored = true;
    const gapTop = p.gapCenter + p.gapHeight / 2;
    const gapBot = p.gapCenter - p.gapHeight / 2;
    const distToEdge = Math.min(Math.abs(w.bird.y - gapTop), Math.abs(w.bird.y - gapBot));
    const nearMiss = distToEdge < NEAR_MISS_MARGIN;
    w.combo += nearMiss ? 2 : 1;
    const points = multiplier(w.combo);
    const before = w.score;
    w.score += points;
    if (Math.floor(w.score / FEATHER_EVERY_POINTS) > Math.floor(before / FEATHER_EVERY_POINTS)) {
      w.feathersRun++;
    }
    events.push({ pipeId: p.id, nearMiss, points });
  }
  return events;
}
```

- [ ] **Step 5: Pass + commit** `feat(core): scoring, combo multiplier, feather cadence`

### Task 7: TimeSystem + SnapshotBuffer (rewind core)

**Files:**
- Create: `src/core/time.ts`, `src/core/snapshots.ts`
- Test: `src/core/time.test.ts`, `src/core/snapshots.test.ts`

**Interfaces:**
- Produces:
  - `class TimeSystem { scale: number; frozen: boolean; update(realDt: number): void; triggerSlowmo(): void; hitstop(ms: number): void }` — exponential ease tau=SLOWMO_EASE_S toward target (SLOWMO_SCALE while hold remains, else 1).
  - `class SnapshotBuffer { record(w: World): void; canRewind(): boolean; rewindInto(w: World): boolean }` — deep-copies mutable fields into plain snapshots; capacity BUFFER_LEN.

- [ ] **Step 1: Failing tests**

```ts
// src/core/time.test.ts
import { describe, it, expect } from "vitest";
import { TimeSystem } from "./time";
import { SLOWMO_SCALE, SLOWMO_HOLD_S } from "./constants";

describe("TimeSystem", () => {
  it("eases into slowmo then back to 1", () => {
    const t = new TimeSystem();
    t.triggerSlowmo();
    for (let i = 0; i < 60; i++) t.update(1 / 60); // 1s real
    expect(t.scale).toBeLessThan(0.5);
    expect(t.scale).toBeGreaterThan(SLOWMO_SCALE - 0.05);
    for (let i = 0; i < 240; i++) t.update(1 / 60); // hold expires, ease back
    expect(t.scale).toBeGreaterThan(0.98);
  });
  it("hitstop freezes exactly ms then unfreezes", () => {
    const t = new TimeSystem();
    t.hitstop(60);
    t.update(0.05);
    expect(t.frozen).toBe(true);
    t.update(0.02);
    expect(t.frozen).toBe(false);
  });
  it("slowmo hold lasts SLOWMO_HOLD_S real seconds", () => {
    const t = new TimeSystem();
    t.triggerSlowmo();
    let elapsed = 0;
    while (elapsed < SLOWMO_HOLD_S + 2 && t.scale < 0.9) { t.update(1 / 120); elapsed += 1 / 120; }
    expect(elapsed).toBeGreaterThanOrEqual(SLOWMO_HOLD_S - 0.05);
  });
});
```

```ts
// src/core/snapshots.test.ts
import { describe, it, expect } from "vitest";
import { SnapshotBuffer } from "./snapshots";
import { createWorld } from "./types";
import { flap } from "./physics";
import { advance } from "./spawner";
import { processPasses } from "./scoring";
import { DT, BUFFER_LEN } from "./constants";

describe("SnapshotBuffer", () => {
  it("rewindInto restores exact world state from 180 ticks ago", () => {
    const w = createWorld(123);
    const buf = new SnapshotBuffer();
    for (let i = 0; i < 300; i++) {
      buf.record(w);
      if (i % 37 === 0) flap(w);
      advance(w, DT);
      processPasses(w);
      w.tick++;
    }
    const saved = JSON.parse(JSON.stringify({
      tick: w.tick, dist: w.dist, score: w.score, combo: w.combo,
      birdY: w.bird.y, birdVy: w.bird.vy, rngState: w.rngState,
      pipeCount: w.pipes.length,
    }));
    expect(buf.canRewind()).toBe(true);
    expect(buf.rewindInto(w)).toBe(true);
    expect(w.tick).toBe(saved.tick - BUFFER_LEN);
    expect(w.bird.y).toBeCloseTo(saved.birdY, 10);
    // deterministic replay forward reproduces identical future
    for (let i = 0; i < 50; i++) { advance(w, DT); w.tick++; }
    const w2 = createWorld(123);
    const buf2 = new SnapshotBuffer();
    for (let i = 0; i < 300; i++) {
      buf2.record(w2);
      if (i % 37 === 0) flap(w2);
      advance(w2, DT);
      processPasses(w2);
      w2.tick++;
    }
    for (let i = 0; i < BUFFER_LEN + 50; i++) { advance(w2, DT); w2.tick++; }
    expect(w.rngState).toBe(w2.rngState);
    expect(w.score).toBeGreaterThanOrEqual(0);
  });
  it("cannot rewind before buffer is full", () => {
    const w = createWorld(1);
    const buf = new SnapshotBuffer();
    buf.record(w);
    expect(buf.canRewind()).toBe(false);
    expect(buf.rewindInto(w)).toBe(false);
  });
});
```

- [ ] **Step 2–4:** verify fail → implement:

```ts
// src/core/time.ts
import { SLOWMO_SCALE, SLOWMO_HOLD_S, SLOWMO_EASE_S } from "./constants";

export class TimeSystem {
  scale = 1;
  frozen = false;
  private target = 1;
  private holdLeft = 0;
  private freezeLeft = 0;

  update(realDt: number): void {
    if (this.freezeLeft > 0) {
      this.freezeLeft -= realDt;
      this.frozen = this.freezeLeft > 0;
    }
    if (this.holdLeft > 0) {
      this.holdLeft -= realDt;
      this.target = this.holdLeft > 0 ? SLOWMO_SCALE : 1;
    }
    const k = 1 - Math.exp(-realDt / SLOWMO_EASE_S);
    this.scale += (this.target - this.scale) * k;
  }

  triggerSlowmo(): void {
    this.holdLeft = SLOWMO_HOLD_S;
  }

  hitstop(ms: number): void {
    this.freezeLeft = ms / 1000;
    this.frozen = true;
  }
}
```

```ts
// src/core/snapshots.ts
import { BUFFER_LEN } from "./constants";
import type { World } from "./types";

interface Snap {
  s: World; // shallow container replaced wholesale; arrays deep-copied below
}

export class SnapshotBuffer {
  private buf: World[] = [];

  record(w: World): void {
    this.buf.push(cloneWorld(w));
    if (this.buf.length > BUFFER_LEN) this.buf.shift();
  }

  canRewind(): boolean {
    return this.buf.length >= BUFFER_LEN;
  }

  /** Restores oldest snapshot (BUFFER_LEN ticks ago) into w. */
  rewindInto(w: World): boolean {
    if (!this.canRewind()) return false;
    const snap = this.buf.shift()!;
    Object.assign(w, cloneWorld(snap));
    this.buf = [];
    return true;
  }
}

function cloneWorld(w: World): World {
  return {
    ...w,
    bird: { ...w.bird },
    pipes: w.pipes.map((p) => ({ ...p })),
    orbs: w.orbs.map((o) => ({ ...o })),
    spawnHistory: [...w.spawnHistory],
  };
}
```

Remove the unused `Snap` interface. Note `Object.assign` keeps object identity of `w` (views hold references).

- [ ] **Step 5: Full suite green + commit** `feat(core): time system (slow-mo/hitstop) + 1.5s rewind buffer`

## Phase 2 — Render Layer

### Task 8: Renderer bootstrap + portrait camera rig

**Files:**
- Create: `src/render/scene.ts`, `src/render/camera.ts`
- Modify: `src/main.ts`

**Interfaces:**
- Produces: `createScene(container: HTMLElement): SceneCtx` returning `{ renderer, scene, setSize(w,h) }` (WebGLRenderer, pixelRatio≤2, hemisphere + directional lights, ground plane mesh 40×40, fog `new THREE.Fog(color, 25, 60)`); `createCameraRig(aspectProvider)` exposing `{ camera, update(dt, birdY, fovKickAmt), kick(amount) }` — portrait FOV fit: `fov = aspect < 1 ? 74 : 60` set on resize; camera at `(6, birdY*0.35 + 3.2, 11)` lookAt `(0, birdY*0.5, 0)` smoothed with damp factor 5/s; FOV kick decays `kick *= exp(-6*dt)` added to base fov.

- [ ] **Steps:** implement scene/camera per signatures above; wire `main.ts` to render an empty lit scene; manual harness check: `npm run dev` shows lit ground plane in portrait window; resize keeps framing; commit `feat(render): scene bootstrap + portrait chase cam`.

### Task 9: Day/night palette (pure fn) + sky dome

**Files:**
- Create: `src/core/palette.ts`, `src/render/sky.ts`
- Test: `src/core/palette.test.ts`

**Interfaces:**
- Produces (core, tested): `dayNight(score: number): PaletteOut` where `PaletteOut = { skyTop: number; skyBottom: number; fogColor: number; sunAngle: number; starAlpha: number }` — cycle length 80 pts across 4 stops (dawn/day/dusk/night), hex colors lerped componentwise, sun angle sweeps π over the day segment mapping, starAlpha peaks at night.
- Render: `class SkyDome` — big inverted sphere with vertex-color gradient shader material (top/bottom uniforms), `update(p: PaletteOut)` sets uniforms + star Points opacity.

Palette stops (hex):
```ts
const STOPS = [
  { skyTop: 0x2c3e6b, skyBottom: 0xffb347, fogColor: 0xd9a06b, sunAngle: -0.4, starAlpha: 0.25 }, // dawn
  { skyTop: 0x3aa0ff, skyBottom: 0xbfe8ff, fogColor: 0xcfe8ff, sunAngle: 1.2, starAlpha: 0 },      // day
  { skyTop: 0x1a2350, skyBottom: 0xff7e47, fogColor: 0xb06b8f, sunAngle: 2.8, starAlpha: 0.15 },   // dusk
  { skyTop: 0x070b1e, skyBottom: 0x1c2b52, fogColor: 0x10182e, sunAngle: 4.2, starAlpha: 1 },      // night
];
```
Tests: score 0 == dawn stop exactly; midpoint 10 == componentwise average of dawn/day; score 80 wraps to dawn; starAlpha monotone within night approach.

Sky dome shader sketch:
```glsl
varying vec3 vPos;
// vertex: vPos = position; standard mvp
// fragment:
uniform vec3 cTop; uniform vec3 cBot;
float h = normalize(vPos).y * 0.5 + 0.5;
gl_FragColor = vec4(mix(cBot, cTop, pow(h, 0.8)), 1.0);
```
Stars: THREE.Points, 300 vertices on dome radius 55, additive, opacity = starAlpha. Sun/moon: emissive sphere positioned by `sunAngle` on radius-50 circle in x/y plane; directional light follows.

Commit: `feat(render+core): tested day/night palette + gradient sky dome`.

### Task 10: Bird view

**Files:**
- Create: `src/entities/birdView.ts`

**Interfaces:**
- Consumes: `World.bird` (read-only).
- Produces: `class BirdView { group: THREE.Group; syncFrom(w: World, alpha: number, flapPulse: number): void; onFlap(): void }` — low-poly bird: SphereGeometry body (r=BIRD_VISUAL_RADIUS, flatShaded), ConeGeometry beak, two BoxGeometry wing pivots rotating z ±70° snapping down on flap (spring: `wingT` decays 8/s), group rotation.z from `pitch` degrees→rad, y interpolated between prev/current physics states using alpha. Skin material slots: `setSkin(bodyHex, bellyHex)` (Task 21 uses).

Manual verify: temp harness auto-flaps every 0.8s; wings snap; pitch dives. Commit: `feat(entities): animated low-poly bird view`.

### Task 11: Pipes + orbs pooled views

**Files:**
- Create: `src/entities/pipesView.ts`, `src/entities/pickupsView.ts`

**Interfaces:**
- Produces: `class PipesView { constructor(scene, poolSize = 8); syncFrom(w: World, alpha: number): void }` — each pipe = Group{lowerCyl, upperCyl, lipRing×2} scaled to gapCenter/gapHeight per frame; MeshStandardMaterial green with emissive flash slot `flash(pipeId)` called on pass. `class OrbsView { syncFrom(w: World, time: number): void }` — emissive cyan spheres (clock texture optional: torus ring + hands via thin boxes), bob `y + sin(time*3)*0.2`, hide when taken.
- Perf: reuse geometries/materials across pool; total tris well under budget.

Manual verify: endless-scroll harness shows recycled pipes/orbs. Commit: `feat(entities): pooled pipe + orb views`.

## Phase 3 — Game Assembly

### Task 12: Input

**Files:**
- Create: `src/core/input.ts`

**Interfaces:**
- Produces: `initInput(onFlap: () => void, onFirstGesture: () => void): void` — `pointerdown` on window (passive:false, preventDefault) triggers flap + one-time audio unlock callback; keydown Space same; ignores repeats.

Code:
```ts
export function initInput(onFlap: () => void, onFirstGesture: () => void): void {
  let gestured = false;
  const fire = () => {
    if (!gestured) { gestured = true; onFirstGesture(); }
    onFlap();
  };
  window.addEventListener("pointerdown", (e) => { e.preventDefault(); fire(); }, { passive: false });
  window.addEventListener("keydown", (e) => {
    if (e.code === "Space" && !e.repeat) { e.preventDefault(); fire(); }
  });
}
```
Also `document.body.style.cssText += ";touch-action:none;-webkit-user-select:none;user-select:none;"` in main.ts. Commit: `feat(core): zero-latency tap input`.

### Task 13: State machine + game loop wiring

**Files:**
- Create: `src/core/game.ts`, `src/core/loop.ts`
- Modify: `src/main.ts`

**Interfaces:**
- Produces: `type GameState = "menu" | "playing" | "hitstop" | "rewindReplay" | "rewindChoice" | "gameOver"`; `class Game { state; time: TimeSystem; world: World; buf: SnapshotBuffer; start(seed?): void; doFlap(): void; chooseRewind(): void; acceptDeath(): void; frame(nowMs: number): void }` owning systems + views; loop accumulator:

```ts
// core/loop.ts
import { DT } from "./constants";
export function makeAccumulator() {
  let acc = 0;
  return {
    step(realDtScaled: number, frozen: boolean, fixed: (dt: number) => void) {
      if (frozen) return;
      acc = Math.min(acc + realDtScaled, 0.25);
      while (acc >= DT) { fixed(DT); acc -= DT; }
      return acc / DT; // alpha for interpolation
    },
  };
}
```
Fixed step during `playing`: `w.scrollSpeed = scrollForScore(w.score); advance(w, dt); stepBird(w, dt); buf.record(w); hits = checkCollisions(w); passes = processPasses(w); w.tick++`. On hit: `bird.alive=false; time.hitstop(60); state='hitstop'` → juice hooks (Task 14). `frame()` calls `time.update(realDt)` first, views sync with returned alpha, renderer renders.

Manual verify: playable slice — tap to flap through pipes, die on contact. Commit: `feat(game): state machine + fixed-step loop assembly`.

### Task 14: Death flow v1 + screen shake

**Files:**
- Create: `src/systems/juice.ts`, `src/ui/gameover.ts`

**Interfaces:**
- Produces: `class Juice { trauma: number; shake(dt): {ox,oy}; burst(pos): void; popup(text): void; fovKick(amount): void }` — trauma-based shake: offset = Perlin-ish `sin` mix × trauma², decay 1.2/s, cap 1; particle pool (≤200 sprites, feather-colored planes tumbling). `ui/gameover.ts`: DOM overlay show(score, best, isNewBest) with Retry button wired to `game.start()`.
Wire: hitstop expiry → if rewind available → `'rewindChoice'` (Task 17 UI stub shows Accept-only until then) else `'gameOver'`.

Verify: death → freeze beat → shake → overlay. Commit: `feat(juice+ui): trauma shake, particles, game-over flow`.

### Task 15: HUD

**Files:**
- Create: `src/ui/hud.ts`, modify `src/style.css`

**Interfaces:**
- Produces: `initHud(): HudApi` with `setScore(n)`, `setCombo(n)`, `setFeathers(n)`, `setSlowmoMeter(frac01)`, `showMenu()`, `hideMenu()`. DOM structure: absolutely-positioned overlay, big score top-center (`font-variant-numeric: tabular-nums`), combo badge under it, feather count top-right, slowmo meter bar top edge, menu panel center ("TAP TO START"), all padding `env(safe-area-inset-*)`, buttons ≥56px tall bottom-third. CSS: `#hud { pointer-events: none } .btn { pointer-events: auto }`.

Verify: readable on 375×667 viewport. Commit: `feat(ui): mobile-first HUD overlay`.

## Phase 4 — Time Dimension Gameplay

### Task 16: Slow-mo orb collection

**Files:**
- Modify: `src/core/game.ts` (fixed step adds pickup check), `src/ui/hud.ts` (meter)
- Create: none new

Logic in playing step: for each untaken orb, if `(orb.x-BIRD_X)² + (orb.y-bird.y)² < 0.9²` → `taken=true; time.triggerSlowmo(); juice.popup("SLOW-MO"); audio.collect()`. HUD meter each frame: `frac = time.scale < 0.95 ? inverseMapRemaining : 0` — expose `TimeSystem.slowmoRemaining(): number` (holdLeft clamped ≥0). Verify: collecting visibly slows world; controls crisp; meter drains in real time. Commit: `feat(time): bullet-time orbs + HUD meter`.

### Task 17: Rewind choice flow

**Files:**
- Modify: `src/core/game.ts`, `src/ui/hud.ts` (choice panel)

Flow: hitstop ends → if `w.feathersRun > 0 && w.rewindsUsedRun < REWINDS_MAX_PER_RUN && buf.canRewind()` → state `rewindReplay`: for ~0.9s real, feed `buf` snapshots backward through views (render oldest→newest reversed; simple: each frame call `view.syncFrom(snapshotWorld, 0)` popping one snapshot) → state `rewindChoice` shows panel [REWIND −🪶] [GIVE UP]. ChooseRewind: `buf.rewindInto(w); w.bird.alive=true; w.bird.invulnUntilTick=w.tick+INVULN_TICKS; w.feathersRun--; w.rewindsUsedRun++; state='playing'` (+ shimmer: bird material opacity pulse while `tick<invulnUntilTick`). GiveUp: gameOver. Persist net feathers at gameOver via storage (Task 21 wires balance; here keep runtime only).

Edge cases tested manually: rewind unavailable → straight to gameOver; double-tap during replay guarded by state checks. Commit: `feat(time): death rewind — ghost replay, choice, invuln resume`.

### Task 18: Live day/night integration

Modify `core/game.ts` frame: `sky.update(dayNight(w.score))` + directional light position from `sunAngle` + fog color. Milestone tint pulse: at `score % MILESTONE_EVERY === 0` cross, `juice.fovKick(2)` + confetti (Task 20 hook stub ok). Verify: long run visibly travels dawn→night. Commit: `feat(render): live day/night driven by score`.

## Phase 5 — Juice & Meta

### Task 19: Procedural audio

**Files:**
- Create: `src/core/audio.ts`

**Interfaces:**
- Produces: `class AudioSys { unlock(): void; flap(): void; score(combo: number): void; collect(): void; nearMiss(): void; die(): void; rewind(): void; milestone(): void; setMuted(b): void }`. Recipes (all short envelopes, master gain 0.5):
  - flap: white-noise buffer 80ms through bandpass sweeping 400→900Hz, gain env 0.25→0.
  - score(combo): sine osc, freq `520 * 2^(min(combo,24)/24)`, 120ms, quick decay. Pitch escalates with combo.
  - collect: triangle arpeggio 660/880/1320Hz, 60ms steps.
  - nearMiss: high sine blip 1500Hz 60ms + tiny noise tick.
  - die: sawtooth drop 220→55Hz over 300ms + noise thud lowpass 200Hz.
  - rewind: noise through bandpass sweeping DOWN 1200→300Hz over 700ms (reverse feel).
  - milestone: major arpeggio C-E-G-C 90ms steps, square wave, quiet.
Unlock resumes AudioContext on first gesture. Commit: `feat(audio): fully procedural WebAudio SFX`.

### Task 20: Popups, confetti, near-miss wiring

Modify `systems/juice.ts` + `core/game.ts`: on PassEvent → popup(`+${points}`), near-miss → popup("CLOSE!") + `audio.nearMiss()` + 80ms `scale` dip via `time.triggerMicroFlash()` (add method: holdLeft-style 0.08s at scale 0.7); milestone crossing → confetti burst (reuse particle pool, rainbow colors) + `audio.milestone()`. Pool discipline: recycle sprites, never allocate in loop. Commit: `feat(juice): popups, micro-flash, milestone confetti`.

### Task 21: Storage + streak + skins

**Files:**
- Create: `src/core/storage.ts` (+ test), `src/ui/menu.ts`

Schema (all JSON localStorage, prefix `f3d.`):
```ts
best: number; feathers: number /*cap 9*/; muted: boolean;
streak: { lastDay: string /*YYYY-MM-DD local*/; count: number };
skin: string; unlocked: string[];
```
API: `loadAll(): SaveData` (defensive defaults), `saveBest(n)`, `bankFeathers(runEarned)` (adds, caps 9, called on run end incl. spent rewinds accounting: bank `runEarned − rewindsUsed` min 0), `touchStreak(todayStr)` (increments when yesterday, resets otherwise), `unlockFor(best)` (thresholds 15/30/50 → skins `'sunrise'|'ember'|'void'`, base `'classic'` always), `setSkin(id)`, `setMuted(b)`. Tests: cap enforcement, streak reset/consecutive logic, unlock thresholds.

Menu UI: title, best/streak display, skin carousel (left/right chips), mute toggle, TAP TO START. Wire game-over → `bankFeathers` + `saveBest` + `touchStreak` + `unlockFor`. Commit: `feat(meta): persistence, daily streak, skin unlocks`.

## Phase 6 — Mobile Polish + Ship

### Task 22: Mobile hardening + PWA

Viewport meta already set (Task 0). Add: `manifest.webmanifest` (name Flappy3D, `display: fullscreen`, `orientation: portrait`, maskable icon 512 — generate simple PNG programmatically via canvas script once, commit binary), theme-color meta. Handle `resize`/`orientationchange` mid-run: renderer.setSize + camera aspect recompute (already in scene API). Double-check iOS quirks: `-webkit-tap-highlight-color: transparent`, prevent context menu (`contextmenu` preventDefault), prevent scroll bounce (`overscroll-behavior: none`). Verify checklist: no rubber-band scroll, no zoom on double-tap, notch insets respected. Commit: `feat(pwa): manifest, icons, mobile input hardening`.

### Task 23: Final verification + README

- [ ] `npx vitest run` — full suite green
- [ ] `npm run build` — production bundle builds; note size (<300KB gz target)
- [ ] Manual QA (desktop Chrome + phone Safari/Android Chrome): 60fps sustained 2min run; tap latency imperceptible; sound only after first gesture; rewind saves from certain death; feathers bank correctly across runs; day/night arc visible by score ~80; no console errors/warnings.
- [ ] README: run/dev/test commands, controls, screenshot.
Commit: `docs: README + final QA pass`.

---

## Self-Review (done at plan-writing time)

- Spec coverage: physics §1 → T2–T6; time §2 → T7/T16/T17/T18; juice §3 → T14/T15/T19/T20; meta §3 → T21; architecture §4 → T13; mobile → T12/T15/T22; testing → throughout + T23. No gaps.
- Placeholders: none — every task has files, interfaces, code or exact recipes, verification, commit.
- Type consistency: single source of truth = `core/types.ts` + `core/constants.ts`; later tasks consume only names defined there or earlier in-plan.
